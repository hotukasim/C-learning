#include<stdio.h>//   এটা stdio.h header-এর information program-এ include করে।

int main(){      //  main() হলো special function। সাধারণভাবে program execution এখান থেকে শুরু হয়।
                 //  main  কোনো argument নেই
                 // Argument = function call করার সময় function-কে যে actual value দেওয়া হয়।
                // main calls printf()

    printf("Hello,Horld\n"); // এটি একটি statement
                        // Statement = computer-কে একটি কাজ করার নির্দেশ।
                        //Statement = একটা কাজের instruction
                        // main function, library থেকে পাওয়া printf() function-কে call করছে।
                        // main() → calls → printf()


}